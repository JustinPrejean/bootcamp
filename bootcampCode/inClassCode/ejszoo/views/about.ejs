<%- include('./partials/header', {title: 'About'}) -%>
<%- include('./partials/navbar') -%>

<h1>I am the about page</h1>

<% zoo.forEach(el => { %>
<% let {name, size, breed, country} = el %>
<p><%= name %> is a(n) <%= size %> <%= breed %> from <%= country %>.</p>
<% }) %>

<%- include('./partials/footer') -%>